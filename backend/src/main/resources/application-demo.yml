# Local testing only. Unlike the old in-memory database, this survives restarts.
spring:
  datasource:
    url: jdbc:h2:file:./data/laundry-demo;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password: ""
server:
  address: 127.0.0.1
app:
  admin:
    username: admin
    password: DemoLaundry123!
  whatsapp:
    enabled: false
